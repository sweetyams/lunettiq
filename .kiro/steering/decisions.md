Significant arch/tech decision made during implementation → append to `.kiro/docs/decisions.md`.

Significant = chooses between viable approaches, introduces new pattern, changes data flow, adds/removes dependency, alters auth/permissions/security.

Format: `### ADR-NNN: Title` + date, status, context, decision, consequences. Increment NNN from last entry. Newest first.

Skip routine choices (variable names, component structure, CSS).
